-> Criando amb virtual
python -m venv venv

-> Ativando o amb virtual
source ./venv/bin/activate


-> No seu python local:
pip freeze > requirements.txt
-> Insira teu arquivo no amb RStudio

No terminal do amb virtual:
pip install -r requirements.txt